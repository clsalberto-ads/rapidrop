{
  "name": "@rapidrop/api-client",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf node_modules"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.62.0",
    "@rapidrop/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
